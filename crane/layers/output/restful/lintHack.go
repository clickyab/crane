package restful

import (
	"fmt"

	"github.com/clickyab/services/assert"
)

func init() {
	if false {

		assert.Nil(parse(&render{}, nil, nil))

		a := restAd{}
		fetchLanding("asd")
		fmt.Println(a)
	}

}
